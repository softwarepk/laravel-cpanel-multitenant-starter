# View rules

- Preserve the shared UI design system for tenant and central screens.
- Prefer `x-ui.*` components and Flux UI.
- Keep the Control Center information-dense and operationally clear.
- Never display infrastructure secrets in views.
- Update `/patterns` when adding a reusable generic tenant UI pattern.
