<x-layouts::auth :title="__('Verify email')"><div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><h1 class="text-2xl font-semibold">{{ __('Verify your email') }}</h1><p class="mt-2 text-sm text-zinc-500">{{ __('Check your inbox for the verification link.') }}</p><form method="POST" action="{{ route('verification.send') }}" class="mt-6">@csrf<flux:button type="submit" variant="primary" class="w-full">{{ __('Resend verification email') }}</flux:button></form></div></x-layouts::auth>
