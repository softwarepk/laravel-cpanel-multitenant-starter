<x-layouts::auth :title="__('Sign in')"><div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><h1 class="text-2xl font-semibold">{{ __('Sign in') }}</h1><p class="mt-2 text-sm text-zinc-500">{{ __('Use an account that belongs to this tenant.') }}</p>@if($errors->any())<div class="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.store') }}" class="mt-6 space-y-4">@csrf<flux:input name="email" type="email" :label="__('Email')" :value="old('email')" required autofocus /><flux:input name="password" type="password" :label="__('Password')" required viewable /><flux:button type="submit" variant="primary" class="w-full">{{ __('Sign in') }}</flux:button></form><div class="mt-4 flex justify-between text-sm">@if(Route::has('password.request'))<a href="{{ route('password.request') }}" class="text-blue-600">{{ __('Forgot password?') }}</a>@endif @if(Route::has('register'))<a href="{{ route('register') }}" class="text-blue-600">{{ __('Register') }}</a>@endif</div></div></x-layouts::auth>
