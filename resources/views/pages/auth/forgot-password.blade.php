<x-layouts::auth :title="__('Forgot password')"><div class="rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><h1 class="text-2xl font-semibold">{{ __('Reset password') }}</h1><p class="mt-2 text-sm text-zinc-500">{{ __('We will send a reset link to your email address.') }}</p>@if(session('status'))<div class="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}" class="mt-6 space-y-4">@csrf<flux:input name="email" type="email" :label="__('Email')" required /><flux:button type="submit" variant="primary" class="w-full">{{ __('Email reset link') }}</flux:button></form></div></x-layouts::auth>
