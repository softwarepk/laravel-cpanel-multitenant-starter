@extends('central.layouts.app', ['title' => 'Settings', 'pageTitle' => 'Settings', 'pageEyebrow' => 'Platform configuration'])
@section('content')
<div class="mb-8"><p class="text-sm font-medium text-zinc-500">Platform administration</p><h1 class="mt-1 text-3xl font-semibold tracking-tight">Control Center settings</h1><p class="mt-2 text-sm text-zinc-500">Application-level policy is stored centrally; hosting credentials remain environment configuration.</p></div>
<div class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_380px]"><form method="POST" action="{{ route('central.settings.update') }}" class="rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900/60">@csrf @method('PUT')<h2 class="font-semibold">Security policy</h2><p class="mt-1 text-sm text-zinc-500">Applied to tenant password validation in production.</p><div class="mt-5 max-w-sm"><label for="password_min_length" class="text-sm font-medium">Minimum password length</label><input id="password_min_length" name="password_min_length" type="number" min="8" max="64" value="{{ old('password_min_length',$passwordMinimumLength) }}" class="mt-2 w-full rounded-xl border border-zinc-300 bg-white px-3.5 py-2.5 dark:border-zinc-700 dark:bg-zinc-950"></div><button class="mt-5 rounded-xl bg-zinc-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-zinc-950">Save settings</button></form><aside class="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900/60"><h2 class="font-semibold">Hosting configuration</h2><dl class="mt-4 space-y-4 text-sm"><div><dt class="text-xs text-zinc-500">Tenant platform domain</dt><dd class="mt-1 font-mono break-all">{{ $platformDomain ?: 'Not configured' }}</dd></div><div><dt class="text-xs text-zinc-500">Custom-domain DNS target</dt><dd class="mt-1 font-mono break-all">{{ $customDomainDnsTarget ?: 'Not configured' }}</dd></div><div><dt class="text-xs text-zinc-500">Document root</dt><dd class="mt-1 font-mono break-all">{{ $platformDocumentRoot ?: 'Not configured' }}</dd></div><div><dt class="text-xs text-zinc-500">cPanel API host</dt><dd class="mt-1 font-mono break-all">{{ $cpanelApiHost ?: 'Not configured' }}</dd></div><div><dt class="text-xs text-zinc-500">API timeout</dt><dd class="mt-1">{{ $cpanelApiTimeout }} seconds</dd></div></dl><p class="mt-5 text-xs leading-5 text-zinc-500">Secrets such as the cPanel API token and database password are intentionally configured only through the environment.</p></aside></div>
@endsection
