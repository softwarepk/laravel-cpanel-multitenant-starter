@extends('central.layouts.app', ['title' => 'Tenants', 'pageTitle' => 'Tenants', 'pageEyebrow' => 'Organizations and tenant lifecycle'])
@section('content')
<div class="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-medium text-zinc-500">Tenant directory</p><h1 class="mt-1 text-3xl font-semibold tracking-tight">Organizations</h1><p class="mt-2 text-sm text-zinc-500">Manage status, platform access, domains, and provisioning.</p></div><a href="{{ route('central.tenants.create') }}" class="rounded-xl bg-zinc-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-zinc-950">New tenant</a></div>
<section class="overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-zinc-800 dark:bg-zinc-900/60"><div class="border-b border-zinc-200 px-5 py-4 dark:border-zinc-800"><div class="flex justify-between"><div><h2 class="font-semibold">All tenants</h2><p class="mt-1 text-xs text-zinc-500">{{ $tenants->count() }} registered</p></div><div class="text-xs text-zinc-500">Platform: <span class="font-mono">{{ $platformDomain ?: 'Not configured' }}</span></div></div></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-zinc-200 text-sm dark:divide-zinc-800"><thead class="bg-zinc-50 text-left text-xs text-zinc-500 dark:bg-zinc-950"><tr><th class="px-5 py-3">Organization</th><th class="px-5 py-3">Primary domain</th><th class="px-5 py-3">Database</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Provisioning</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-zinc-100 dark:divide-zinc-800">@forelse($tenants as $tenant)@php($primary=$tenant->domains->firstWhere('is_primary',true)??$tenant->domains->first())<tr><td class="px-5 py-4"><div class="font-semibold">{{ $tenant->name ?: $tenant->id }}</div><div class="font-mono text-xs text-zinc-500">{{ $tenant->id }}</div></td><td class="px-5 py-4">{{ $primary?->domain ?: '—' }}</td><td class="px-5 py-4 font-mono text-xs">{{ $tenant->database_name ?: '—' }}</td><td class="px-5 py-4"><x-ui.status-badge :status="$tenant->status" /></td><td class="px-5 py-4"><x-ui.status-badge :status="$tenant->provisioning_status" /></td><td class="px-5 py-4 text-right"><a href="{{ route('central.tenants.show',$tenant) }}" class="font-medium">Manage</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-14 text-center text-zinc-500">No tenants yet. <a href="{{ route('central.tenants.create') }}" class="font-medium text-blue-600">Create the first tenant.</a></td></tr>@endforelse</tbody></table></div></section>
@endsection
