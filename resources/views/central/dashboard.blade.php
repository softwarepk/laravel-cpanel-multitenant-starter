@extends('central.layouts.app', ['title' => 'Control Center', 'pageTitle' => 'Dashboard', 'pageEyebrow' => 'Central Administration'])
@section('content')
<div class="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-medium text-zinc-500">Platform overview</p><h1 class="mt-1 text-3xl font-semibold tracking-tight">Operational control at a glance</h1><p class="mt-2 text-sm text-zinc-500">Monitor tenant health, custom domains, and recent administrative activity.</p></div><a href="{{ route('central.tenants.create') }}" class="rounded-xl bg-zinc-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-zinc-950">New tenant</a></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Total tenants',$totalTenants,'All registered organizations'],['Active tenants',$activeTenants,'Provisioned and available'],['Needs attention',$attentionTenants,'Failed or incomplete provisioning'],['Suspended',$suspendedTenants,'Access currently disabled']] as [$label,$value,$detail])<div class="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-800 dark:bg-zinc-900/60"><div class="text-sm font-medium text-zinc-500">{{ $label }}</div><div class="mt-3 text-3xl font-semibold">{{ $value }}</div><div class="mt-2 text-xs text-zinc-500">{{ $detail }}</div></div>@endforeach</div>
<div class="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1.35fr)_minmax(360px,.65fr)]"><section class="overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-zinc-800 dark:bg-zinc-900/60"><div class="flex items-center justify-between border-b border-zinc-200 px-5 py-4 dark:border-zinc-800"><div><h2 class="font-semibold">Recently added tenants</h2><p class="mt-1 text-xs text-zinc-500">Latest organizations onboarded to the platform.</p></div><a href="{{ route('central.tenants.index') }}" class="text-sm font-medium">View all</a></div><div class="divide-y divide-zinc-100 dark:divide-zinc-800">@forelse($recentTenants as $tenant)@php($primary=$tenant->domains->firstWhere('is_primary',true)??$tenant->domains->first())<a href="{{ route('central.tenants.show',$tenant) }}" class="flex items-center justify-between gap-4 px-5 py-4 hover:bg-zinc-50 dark:hover:bg-zinc-900"><div><div class="font-semibold">{{ $tenant->name ?: $tenant->id }}</div><div class="mt-1 text-xs text-zinc-500">{{ $primary?->domain ?: $tenant->id }}</div></div><x-ui.status-badge :status="$tenant->status" /></a>@empty<div class="px-5 py-12 text-center text-sm text-zinc-500">No tenants registered yet.</div>@endforelse</div></section><div class="space-y-6"><section class="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900/60"><h2 class="font-semibold">Custom domains</h2><div class="mt-5 grid grid-cols-2 gap-3"><div class="rounded-xl bg-zinc-50 p-4 dark:bg-zinc-950"><div class="text-2xl font-semibold">{{ $activeCustomDomains }}</div><div class="text-xs text-zinc-500">Active</div></div><div class="rounded-xl bg-zinc-50 p-4 dark:bg-zinc-950"><div class="text-2xl font-semibold">{{ $pendingCustomDomains }}</div><div class="text-xs text-zinc-500">Pending</div></div></div></section><section class="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900/60"><div class="flex justify-between"><h2 class="font-semibold">Recent activity</h2><a href="{{ route('central.activity.index') }}" class="text-xs text-zinc-500">Full log</a></div><div class="mt-4 space-y-4">@forelse($recentActivity as $log)<div><div class="text-sm font-medium">{{ $log->description ?: $log->action }}</div><div class="mt-1 text-xs text-zinc-500">{{ $log->centralAdmin?->name ?: 'System' }} · {{ $log->created_at?->diffForHumans() }}</div></div>@empty<div class="text-sm text-zinc-500">No activity yet.</div>@endforelse</div></section></div></div>
@endsection
