@props(['value' => 20])
<div class="flex items-center gap-2 text-sm text-zinc-500"><span>{{ __('Per page') }}</span><span class="font-medium text-zinc-800 dark:text-zinc-200">{{ $value }}</span></div>
