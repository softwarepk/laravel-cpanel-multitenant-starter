# UI Guide

The multi-tenant starter uses the same UI system as `laravel-cpanel-starter` for both tenant applications and the central Control Center.

See `docs/UI-DESIGN-SYSTEM.md` and the tenant `/patterns` gallery. Reuse the existing `x-ui.*` components and Flux patterns before creating new variants.
